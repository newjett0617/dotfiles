### z shell environmental variables 環境變數
### zsh 啟動時加載順序()
### /etc/zshenv # 所有 zsh 實例啟動時都會加載
### ~/.zshenv # 所有 zsh 實例啟動時都會加載
### /etc/zprofile # 當前 shell 為 login shell 時才會加載
### ｜- /etc/paths
### ｜- /etc/paths.d/
### ~/.zprofile # 當前 shell 為 login shell 時才會加載
### /etc/zshrc # 當前 shell 為 interactive shell 時才會加載
### ~/.zshrc # 當前 shell 為 interactive shell 時才會加載
### /etc/zlogin # 當前 shell 為 login shell 時才會加載
### ~/.zlogin # 當前 shell 為 login shell 時才會加載
###
### # zsh 退出時加載
### ~/.zlogout # 正常退出 login shell 時才會加載
### /etc/zlogout # 正常退出 login shell 時才會加載

# ================================= #

### set locale

